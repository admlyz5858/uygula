# 🔥 FOCUS UNIVERSE

**FOCUS UNIVERSE** is a production-ready immersive Pomodoro platform that blends:

- 🎮 **Gamification** (growth world, XP, levels, quests, streaks)
- 🧘 **Ambient experience** (nature scenes, crossfading soundscape, breath-focused UI)
- 🧠 **AI planning** (goal-to-session planner)
- 📊 **Behavior analytics** (daily/weekly stats + heatmap)
- 📱 **Mobile-first deployment** (PWA + Capacitor Android)

It is designed to feel like **Game + Meditation + Productivity System**, not a plain timer.

---

## ✨ Core Features

### Immersive Focus World
- Start session with immersive transition
- Dynamic nature backgrounds (remote stock + local fallback)
- 5-minute automatic background rotation with smooth crossfade
- Additional idle auto-rotation while app is open
- Ken Burns style subtle zoom and animated particle layer
- Calm/bright mood adaptation between focus and break modes

### Audio Engine
- Mode-specific ambient and instrumental tracks
- Expanded multi-track library for focus/short-break/long-break modes
- Audio crossfade between tracks/modes
- Volume normalization per track
- Last 10 seconds adaptive ticking sound
- Final soft bell signal

### Gamification
- 🌱 Growth system: seed → sprout → tree → magical tree
- 🧠 XP + level-up progression
- 🏆 Daily/weekly quests
- 🔥 Streak tracking
- ⚔ Boss challenge sessions (higher risk/reward)
- 🛡 Distraction Guard (tracks and penalizes focus exits)
- 👥 Focus rooms with social XP bonuses (realtime presence when Supabase is configured)
- 🧩 Break mini activities:
  - Watering plants
  - Rhythm tap
  - Memory puzzle

### Quick Actions
- One-tap quick session starters (15/25/50)
- Boss quick start
- PWA home-screen shortcuts with prefilled quick actions

### AI Planning
- User writes natural-language goal
- Planner splits into Pomodoro blocks
- Session and schedule recommendations generated automatically

### Stats & Psychology
- Daily and weekly session charts
- 12-week heatmap (GitHub-style)
- Productivity score
- Low-stress supportive micro-copy and progress feedback

### Mobile & Platform
- PWA installability + auto-updating Service Worker
- Wake lock attempt on active sessions
- Vibration/haptics feedback
- Capacitor Android packaging ready

### Export
- 4K timer video export (`3840x2160`) via Canvas + MediaRecorder

---

## 🧱 Tech Stack

- **React 19**
- **TypeScript**
- **Vite**
- **Tailwind CSS**
- **Framer Motion**
- **IndexedDB** (`idb`)
- **Supabase Realtime Presence** (optional, for live Focus Rooms)
- **PWA** (`vite-plugin-pwa`)
- **Capacitor Android**
- **Vitest + Testing Library**
- **GitHub Actions CI**

---

## 🚀 Local Development

1) Configure environment (for live Focus Rooms):

```bash
cp .env.example .env
```

Fill:
- `VITE_SUPABASE_URL`
- `VITE_SUPABASE_ANON_KEY`

If omitted, Focus Rooms fall back to static demo counts.

2) Install and run:

```bash
npm install
npm run dev
```

Open:
`http://localhost:5173`

---

## ✅ Tests

```bash
npm run test:run
```

---

## 🏗️ Production Build

```bash
npm run build
npm run preview
```

---

## 📱 Android APK

### Build commands

```bash
npm run sync:android
npm run build:apk
```

Output:

`android/app/build/outputs/apk/debug/app-debug.apk`

---

## 🌐 PWA Notes

- Service worker and manifest are generated by `vite-plugin-pwa`
- Remote stock images are cached with runtime caching
- Local assets are available as fallback when remote sources fail

---

## 🧪 CI

GitHub Actions workflow runs:

1. `npm ci`
2. `npm run test:run`
3. `npm run build`

Workflow file:

`.github/workflows/ci.yml`

---

## 📚 Royalty-Free Media

### Local fallback images and audio

Stored under:

- `public/assets/images`
- `public/assets/music`

These are bundled and used as guaranteed fallback assets.

### Remote stock image sources

- Unsplash image URLs
- Pexels image URLs

Used with automatic preload checks and local fallback.
